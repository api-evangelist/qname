# QName AI WHOIS API

QName AI (qname.ai) is a domain-research workspace whose WHOIS/RDAP capability is exposed programmatically through a small, approval-gated REST API and an agent-native CLI (@qname/cli). It offers authenticated single-domain and batch WHOIS/domain lookups plus a domain-traffic scope, consumable via the CLI, an installable Agent Skill, or direct REST calls.

- **Provider:** https://qname.ai
- **Documentation:** https://qname.ai/cli
- **Base URL:** https://qname.ai
- **Tags:** whois, rdap, domain-research, domain-search, batch-lookup, cli, agent-tooling, agent-skill, llms-txt, developer-tools, domain-intelligence
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 88/100

## APIs

- **QName AI WHOIS REST API** — Approval-gated REST API for WHOIS/RDAP lookups: GET /api/whois/{domain} (single), POST /api/whois/batch (batch), and GET /api/domain-traffic (traffic). Requires an admin-approved API key (qname_ prefix) and enforces per-request and daily quotas. (`https://qname.ai`)

> REST API is real but approval-gated (API key requested at https://qname.ai/settings/apikeys, admin-approved). No public OpenAPI/AsyncAPI/GraphQL/Postman contract found. Agent-native surfaces: llms.txt (present, but does not mention the API) and a published Agent Skill installable via 'npx skills add QNameAI/qname-cli -y -g' and shipped in the @qname/cli npm package. No MCP server (hosted or local) found. Recommended client is the open-source @qname/cli. AgentSkill URL points to the GitHub repo where the skill lives (skills/qname-cli/) since no dedicated skill URL was confirmed. apis.json at https://qname.ai/apis.json returned 403 at research time.

_Generated by apis.io from a community submission._